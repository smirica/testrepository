{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "node scripts/build.js"
  }
}
